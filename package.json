{
    "name": "to-do-list",
    "version": "1.0.0",
    "scripts": {
        "test": "echo 'Test command'",
        "build": "gulp build",
        "deploy": "gulp deploy"
    },
    "dependencies": {
        "gulp": "^4.0.2",
        "gulp-uglify": "^3.0.2",
        "gulp-concat": "^2.6.1"
    }
}
